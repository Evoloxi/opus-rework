package me.evo

import net.fabricmc.api.ClientModInitializer
import java.util.logging.Logger

object OpusReworkClient : ClientModInitializer {

	val LOGGER: Logger = Logger.getLogger(OpusReworkClient::class.java.name)
	override fun onInitializeClient() {
	}
}